# Patch to add CLIENT_SECRET environment variable from Kubernetes Secret
# Apply this after deploying the service: kubectl patch deployment gruppe8-tcc-status-service -n gruppe8-tcc --type='json' -p='[{"op": "add", "path": "/spec/template/spec/containers/0/env/-", "value": {"name": "CLIENT_SECRET", "valueFrom": {"secretKeyRef": {"name": "tcc-status-service-secret", "key": "client-secret"}}}}]'
#
# Or use this as a strategic merge patch:
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gruppe8-tcc-status-service
  namespace: gruppe8-tcc
spec:
  template:
    spec:
      containers:
        - name: gruppe8-tcc-status-service
          env:
            - name: CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: tcc-status-service-secret
                  key: client-secret
