# Logging configuration
quarkus.log.level=INFO

# Since we use Java 21, we need to specify a Docker image
quarkus.jib.base-jvm-image=eclipse-temurin:21

# We use our own local Docker registry and not Docker Hub
quarkus.container-image.insecure=true
quarkus.container-image.registry=localhost:5001
quarkus.container-image.build=true
quarkus.container-image.push=

# General Kubernetes deployment configuration
quarkus.micrometer.export.json.enabled=true
quarkus.micrometer.export.json.path=metrics/json
quarkus.micrometer.export.prometheus.path=metrics/prometheus

# Enable liveness and readiness probes
quarkus.smallrye-health.extensions.enabled=true
# Enable management interface for health checks without mTLS
quarkus.management.enabled=true
quarkus.management.port=9000

# Kubernetes configuration
quarkus.kubernetes.namespace=gruppe8-tcc
quarkus.kubernetes.name=gruppe8-tcc-priority-service
quarkus.kubernetes.replicas=1
# Ingress disabled - using shared Ingress manifest (see kubernetes/ingress.yaml)
quarkus.kubernetes.ingress.expose=false

#--------------------------------
# TLS Configuration (Task 4)
#--------------------------------
# IMPORTANT: Services should only run in Kubernetes, not locally!
# TLS configuration is automatically enabled when deployed to Kubernetes.
# For local development, use port-forwarding or Ingress to access services.
# 
# Kubernetes-only: Mount TLS secret as volume (for deployment manifests)
quarkus.kubernetes.secret-volumes."tls-secret-volume".secret-name=task4-gruppe8-tcc-priority-service-tls
quarkus.kubernetes.secret-volumes."tls-secret-volume".default-mode=0400
quarkus.kubernetes.mounts."tls-secret-volume".path=/etc/certs
quarkus.kubernetes.mounts."tls-secret-volume".read-only=true

# Kubernetes-only: Mount trust-manager CA bundle for mTLS
quarkus.kubernetes.config-map-volumes."trusted-cas-volume".config-map-name=cluster-root-ca
quarkus.kubernetes.mounts."trusted-cas-volume".path=/etc/certs/trust
quarkus.kubernetes.mounts."trusted-cas-volume".read-only=true

# TLS configuration for Kubernetes deployment
# These properties are only effective when the certificates are mounted in Kubernetes
# For local development, services should not run locally
quarkus.kubernetes-config.secrets.enabled=true
quarkus.kubernetes-config.secrets=task4-gruppe8-tcc-priority-service-tls
quarkus.http.insecure-requests=disabled
quarkus.http.ssl.certificate.files=/etc/certs/tls.crt
quarkus.http.ssl.certificate.key-files=/etc/certs/tls.key
quarkus.http.ssl-port=8443
# Configure Kubernetes service to use SSL port
quarkus.kubernetes.ports.http.container-port=8443
quarkus.kubernetes.ports.http.protocol=TCP
# Configure management port for health checks (without mTLS)
quarkus.kubernetes.ports.management.container-port=9000
quarkus.kubernetes.ports.management.protocol=TCP
# Configure health checks to use management port (HTTP, no mTLS)
quarkus.kubernetes.liveness-probe.http-action-port=9000
quarkus.kubernetes.liveness-probe.http-action-scheme=HTTP
quarkus.kubernetes.readiness-probe.http-action-port=9000
quarkus.kubernetes.readiness-probe.http-action-scheme=HTTP
quarkus.kubernetes.startup-probe.http-action-port=9000
quarkus.kubernetes.startup-probe.http-action-scheme=HTTP
quarkus.http.ssl.certificate.trust-store-file=/etc/certs/trust/root-certs.pem
quarkus.http.ssl.certificate.trust-store-type=PEM
quarkus.http.ssl.client-auth=required

