# Kubernetes-specific configuration for TLS
# This file is used when deploying to Kubernetes
# It enables TLS configuration

# Enable Kubernetes config to read secrets
quarkus.kubernetes-config.secrets.enabled=true
quarkus.kubernetes-config.secrets=task4-gruppe8-tcc-status-service-tls

# Configure TLS for HTTP server (enabled in Kubernetes)
quarkus.http.insecure-requests=disabled
quarkus.http.ssl.certificate.files=/etc/certs/tls.crt
quarkus.http.ssl.certificate.key-files=/etc/certs/tls.key

